import assert from "assert";
import { User } from "../users";

describe("Test du virtual type", () => {
  it("*** Test de virtual type count Books ***", (done) => {
    const user1 = new User({
      name: "Matis",
    });
  });
});
